import { getLatestRowsFromSheet, getFullRowsFromSheet } from '../_sheet-latest.js';
import { SHEET_BARANG_KELUAR } from '../_barang-ops.js';
const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:{'Content-Type':'application/json; charset=utf-8'}});
export async function onRequestGet({request,env}){try{const u=new URL(request.url);const mode=String(u.searchParams.get('mode')||'full').toLowerCase();const limit=Number(u.searchParams.get('limit')||1000);const spreadsheetId=env.SHEET_ID_2026;if(mode==='latest'){const {startRow,rows}=await getLatestRowsFromSheet({env,spreadsheetId,sheetName:SHEET_BARANG_KELUAR,columns:'A:I',limit});return json({success:true,mode,startRow,rows:rows.map((row,index)=>({rowNumber:startRow+index,tanggal:row[0]||'',from:row[1]||'',to:row[2]||'',sku:row[3]||'',namaBarang:row[4]||'',qty:row[5]||'',status:row[6]||'',pic:row[7]||'',keterangan:row[8]||''}))});}const values=await getFullRowsFromSheet({env,spreadsheetId,sheetName:SHEET_BARANG_KELUAR,columns:'A:I'});return json({success:true,mode,startRow:2,rows:values.slice(1).map((row,index)=>({rowNumber:index+2,tanggal:row[0]||'',from:row[1]||'',to:row[2]||'',sku:row[3]||'',namaBarang:row[4]||'',qty:row[5]||'',status:row[6]||'',pic:row[7]||'',keterangan:row[8]||''}))});}catch(err){return json({success:false,message:err?.message||'Internal server error'},500);}}
